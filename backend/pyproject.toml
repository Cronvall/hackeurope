[project]
name = "ev-trip-agent"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.115.0",
    "anthropic>=0.40.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.1",
    "sse-starlette>=2.0.0",
]

[tool.uv.sources]
chargetrip-experiment = { path = "./chargetrip_experiment" }
