[project]
name = "chargetrip-experiment"
version = "0.1.0"
description = "Python API interface for Chargetrip GraphQL — routes, stations, operators, amenities"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "python-dotenv>=1.2.1",
]
